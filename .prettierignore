node_modules
dist
build
coverage
pnpm-lock.yaml
# Generated by the TanStack Router plugin on every build.
src/routeTree.gen.ts
# Specs and plans are hand-wrapped prose; prettier reflows their tables and
# creates hundreds of lines of cosmetic churn on reviewed documents.
docs/
# Same reason as docs/: SDD briefs, progress and reports are hand-wrapped prose.
.superpowers/

# Vendored shadcn output. eslint already excludes it as upstream's to own;
# prettier must match, or every `shadcn add` re-add churns the diff on files
# we do not edit. shadcn emits semicolons and double quotes.
# NOTE the trailing `*`, not a trailing `/`: gitignore syntax cannot re-include
# a file whose parent DIRECTORY is excluded, so `src/components/ui/` would make
# the negations below silently do nothing. Excluding the entries instead of the
# directory is what makes them work.
src/components/ui/*
# ...except the files in there that are OURS, not upstream's. sonner.tsx is
# hand-written (the registry one imports next-themes) and form.tsx is written
# by hand in Task 5. Without these negations our own code would sit in the
# directory unformatted and unchecked. form.tsx is listed before it exists so
# it is covered the moment it lands.
!src/components/ui/sonner.tsx
!src/components/ui/form.tsx
